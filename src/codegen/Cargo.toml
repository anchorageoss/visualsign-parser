[package]
name = "codegen"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tonic-build = { version = "0.10", features = ["cleanup-markdown"] }
protobuf-src = { version = "1" }

[lints]
workspace = true
