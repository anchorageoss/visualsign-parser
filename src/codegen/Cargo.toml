[package]
name = "codegen"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tonic-build = { version = "0.9", features = ["cleanup-markdown"] }
prost-build = "0.11"
protobuf-src = { version = "1" }
